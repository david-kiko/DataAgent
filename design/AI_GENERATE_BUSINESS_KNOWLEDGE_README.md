# AI生成业务知识描述功能需求文档

## 功能概述

在前端"编辑业务知识"界面中增加一个"AI生成"按钮，当用户在描述字段中输入SQL语句或其他内容时，点击该按钮可以自动生成结构化的业务知识描述。

## 功能需求

### 1. 触发方式
- **按钮位置**：独立的"AI生成"按钮，放置在描述框右下方
- **交互方式**：点击后弹出确认窗口，用户可以预览生成结果并进行修改
- **确认机制**：用户确认后替换原描述内容

### 2. 输入处理
- **输入位置**：使用现有的描述字段
- **输入内容**：支持任何描述内容，不限于SQL语句
- **灵活性**：用户可以输入SQL、自然语言描述或混合内容

### 3. 业务知识召回逻辑
- **SQL识别**：如果输入内容包含SQL语句，提取涉及的表名和字段
- **Schema召回**：基于表名和字段召回对应的schema信息
- **业务知识召回**：提取关键字，召回相关的业务知识内容
- **组合处理**：将schema信息和业务知识结合使用

### 4. 输出格式
- **动态调整**：根据SQL内容动态生成描述格式
- **结构化描述**：生成包含以下要素的描述：
  - 计算公式
  - 字段说明（含null值处理）
  - 过滤条件
  - 时间过滤字段
  - 涉及表
  - 业务术语解释
- **目标**：生成的描述要能让大模型在用户提问时重新生成对应的SQL

### 5. 错误处理
- **SQL解析**：不进行语法解析，直接交给大模型处理
- **召回失败**：如果无法召回相关内容，让大模型基于输入内容自行生成描述
- **容错机制**：确保在任何情况下都能生成有意义的描述

### 6. 技术实现
- **后端接口**：创建新的API接口，参考现有plan生成逻辑
- **前端交互**：显示转圈等待状态，无需进度条
- **大模型调用**：复用现有的LLM服务

## 实现示例

### 输入示例
```sql
SELECT
COUNT(cid) AS 订单总数,
SUM(CQUALIFIED_QTY + NULLIF(CSCRAPPED_QTY,0)) * 1.0 / NULLIF(SUM(CPLANNED_QTY), 0) AS 生产计划达成率
FROM MOM_PROD_ORDER
WHERE CSOFT_DELETE_FLAG = 0
AND CLIFECYCLE_STATE = 'MO_060_FINISHED'
AND CBIZ_ORG_ID = 20482480100343808
AND CMANU_MODEL = '...'
AND CPLANNED_END_TIME BETWEEN '2025-01-01' AND '2025-12-31 23:59:59';
```

### 输出示例
```
计算公式：((合格数量 + 报废数量) / 计划产出数量) * 100%
合格数量： CQUALIFIED_QTY, 需要使用IFNULL函数处理null值
报废数量： CSCRAPPED_QTY, 需要使用IFNULL函数处理null值
计划产出数量: CPLANNED_OUTPUT_QTY, 需要使用IFNULL函数处理null值
过滤条件： CLIFECYCLE_STATE='PO_060_FINISHED'
时间过滤字段： CPLANNED_END_TIME
涉及表： MOM_PROD_ORDER
分厂就是指的所属组织
```

## 技术架构

### 前端实现
1. 在描述字段右下方添加"AI生成"按钮
2. 点击后显示loading状态，同时禁用按钮防止重复点击
3. 调用后端API获取生成结果
4. 弹出确认窗口展示结果
5. 用户确认后更新描述字段
6. 请求完成后恢复按钮可点击状态

### 后端实现
1. 创建新的Controller接口
2. 实现业务知识召回逻辑
3. 调用LLM服务生成描述
4. 返回结构化结果

### 数据流
```
用户输入 → 前端发送请求 → 后端召回schema和业务知识 → LLM生成描述 → 返回结果 → 用户确认 → 更新描述
```

## 开发计划

### 阶段1：后端接口开发
- [ ] 创建AI生成描述接口
- [ ] 实现SQL解析和表名提取
- [ ] 实现schema召回逻辑
- [ ] 实现业务知识召回逻辑
- [ ] 集成LLM调用

### 阶段2：前端界面开发
- [ ] 添加AI生成按钮
- [ ] 实现loading状态
- [ ] 创建确认弹窗
- [ ] 集成API调用

### 阶段3：测试和优化
- [ ] 功能测试
- [ ] 边界情况处理
- [ ] 用户体验优化

## 注意事项

1. **性能考虑**：LLM调用可能较慢，需要合适的超时设置
2. **用户体验**：确保loading状态清晰，按钮禁用防止重复点击
3. **数据安全**：确保用户输入的内容安全传递给LLM
4. **错误处理**：提供友好的错误提示信息，请求失败时恢复按钮状态
5. **可扩展性**：设计时考虑未来可能的功能扩展

## 相关文件

- 前端：`spring-ai-alibaba-data-agent-frontend/src/views/AgentDetail.vue`
- 后端：需要创建新的Controller和Service
- 业务知识相关：`AgentKnowledgeController.java`, `AgentKnowledgeService.java`
